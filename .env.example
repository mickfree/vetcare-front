# En desarrollo, deja esta variable sin definir para usar el proxy local hacia Django.
# En producción, indica la URL pública completa de la API, incluyendo /api/v1.
PUBLIC_API_BASE_URL=https://api.tu-dominio.com/api/v1
